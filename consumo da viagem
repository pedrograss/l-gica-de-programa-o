distancia = float(input("Digite a distância da viagem: "))
consumo = float(input("Digite o consumo do combustivel em km/l: "))
preco = float(input("Digite o preço do litro do combustivel: "))

calculo1 = (distancia / consumo)
calculo2 = (calculo1 * preco)

print("Você vai gastar um total de: ", calculo2)